specVersion: "v1"
serviceId: "hotel-reservation-f38d7-service"
location: "us-east4"
schemas:
  - source: "./schema"
    datasource:
      postgresql:
        database: "hotel-reservation-f38d7-database"
        cloudSql:
          instanceId: "hotel-reservation-f38d7-instance"
          # schemaValidation: "STRICT"     # STRICT mode makes Postgres schema match Data Connect exactly.
          # schemaValidation: "COMPATIBLE" # COMPATIBLE mode makes Postgres schema compatible with Data Connect.
connectorDirs: ["./example"]
